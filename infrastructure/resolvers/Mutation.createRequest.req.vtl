## AppSync VTL Request Mapping for createRequest mutation
{
  "version": "2017-02-28",
  "operation": "Invoke",
  "payload": {
    "field": "createRequest",
    "arguments": $utils.toJson($context.arguments),
    "identity": {
      "sub": "$context.identity.sub",
      "username": "$context.identity.username",
      "claims": $utils.toJson($context.identity.claims)
    }
  }
}
